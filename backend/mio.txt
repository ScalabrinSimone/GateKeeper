FARE SEMPRE py PRIMA DI OGNI COMANDO!!!

PER INSTALLARE FARE "py -m pip install *nome*" !!!!!!

HIVEMQ () <------------------------------------------------------------USARE

----------------------------------------------------------

py -m pip install *nome*

python -m venv .venv

.venv\Scripts\activate.ps1  <------------------- QUESTO

pip list

ho bisogno che cancelli il database "test.db" e le tabelle perché ti dò definitivamente come deve essere strutturato.
Le tabelle sono:
"users":    # TODO: AGGIUNGERE EMAIL E UUID UNIVOCO
- id INT AUTO_INCREMENT PRIMARY KEY
- username VARCHAR(16) UNIQUE NOT NULL
- password_hash VARCHAR(255) NOT NULL
- created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP

"devices"
- id INT AUTO_INCREMENT PRIMARY KEY
- name VARCHAR(16) NOT NULL
- created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP

"user_device"
- association_id: INTEGER AUTO_INCREMENT PK;
- user_id: INTEGER FK NOT NULL;
- device_id: INTEGER FK NOT NULL.
UNIQUE (user_id, device_id)

FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
FOREIGN KEY (device_id) REFERENCES devices(id) ON DELETE CASCADE

"logs"
- id INT AUTO_INCREMENT PRIMARY KEY,
- user_id INT NOT NULL,
- device_id INT NOT NULL,
- action ENUM('ENTRATO', 'USCITO') NOT NULL,
- created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,

FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
FOREIGN KEY (device_id) REFERENCES devices(id) ON DELETE CASCADE



https://www.perplexity.ai/search/11eb9b4d-d9cf-4362-bd48-4d2d327e3032#4