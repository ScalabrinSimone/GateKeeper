# ── Dipendenze backend GateKeeper ─────────────────────────────────────────────
# Installa con: pip install -r requirements.txt
# Consigliato: usare un virtualenv (python -m venv .venv && source .venv/bin/activate)

# Framework web asincrono
fastapi==0.111.0

# Server ASGI (gira FastAPI)
uvicorn[standard]==0.29.0

# ORM per il database
sqlalchemy==2.0.30

# Validazione settings da env
pydantic-settings==2.2.1

# Hashing password (bcrypt sotto)
passlib[bcrypt]==1.7.4

# JWT (JSON Web Token) per autenticazione
python-jose[cryptography]==3.3.0

# Parsing form data (login OAuth2)
python-multipart==0.0.9

# TODO: aggiungi aiofiles se vuoi upload file
# aiofiles==23.2.1

# TODO: aggiungi asyncpg + databases se passi a PostgreSQL
# asyncpg==0.29.0
# databases[postgresql]==0.9.0

# Runtime diretto del progetto
pydantic==2.7.4
Werkzeug==3.1.3
pyserial==3.5
bleak==0.22.3

# QR-code di pairing stampato in console all'avvio del backend
# (mostra URL hub + factory_code finché l'hub non è accoppiato).
# L'extra [pil] abilita la generazione del PNG fallback su disco.
qrcode[pil]==7.4.2

# Dipendenze runtime usate dal codice o richieste dai pacchetti principali
annotated-types==0.7.0
anyio==4.4.0
click==8.1.7
h11==0.14.0
idna==3.7
typing-extensions==4.12.2

# Compatibile con FastAPI 0.111.0
starlette==0.37.2

# Necessaria su Linux/Raspberry Pi per bleak
# (installata automaticamente su molte piattaforme, ma qui la rendiamo esplicita)
dbus-fast>=2.21.1; sys_platform == "linux"