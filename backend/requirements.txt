# ============================================================
# GateKeeper — Backend Python dependencies
# ============================================================
# Installa tutto con:  pip install -r requirements.txt
# Consigliato: crea prima un virtualenv
#   python -m venv .venv && source .venv/bin/activate

# --- Framework web ---
fastapi==0.111.0          # Framework HTTP async, genera anche la doc OpenAPI
uvicorn[standard]==0.29.0 # Server ASGI (esegue FastAPI); [standard] include websockets+watchfiles

# --- Database ---
sqlalchemy==2.0.30        # ORM: mappa classi Python su tabelle SQL
alembic==1.13.1           # Gestione migrazioni DB (come le "versioni" dello schema)
aiosqlite==0.20.0         # Driver SQLite asincrono (per sviluppo locale)
# TODO: in produzione su Raspberry usa psycopg2-binary per PostgreSQL
# psycopg2-binary==2.9.9

# --- Autenticazione ---
python-jose[cryptography]==3.3.0  # Crea e verifica JWT (JSON Web Token)
passlib[bcrypt]==1.7.4             # Hash delle password con bcrypt

# --- Validazione / Settings ---
pydantic==2.7.1            # Validazione dati (usata internamente da FastAPI)
pydantic-settings==2.2.1   # Legge variabili d'ambiente / file .env in modo tipizzato
python-multipart==0.0.9    # Necessario per ricevere form-data (es. login via form)

# --- Utilità ---
python-dotenv==1.0.1       # Carica il file .env nelle variabili d'ambiente
httpx==0.27.0              # Client HTTP async (utile per i test e per chiamate esterne)
